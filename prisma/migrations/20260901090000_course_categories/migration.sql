-- ФАЗА CAT+, задача CAT+.0.1 (01.09.2026). За прямим проханням
-- користувача — можливість вказувати кілька категорій курсу
-- (нарощення/зняття/чистка/опил/рівні складності або власну довільну
-- категорію). Той самий підхід, що вже `introHighlights` (задача
-- HOME+.0.2) на цій самій моделі — простий Postgres-масив рядків,
-- без нового enum чи окремої таблиці.

-- AlterTable
ALTER TABLE "Course" ADD COLUMN "categories" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[];
