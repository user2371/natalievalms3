-- ADMIN+.1, задача про захист прав адміністратора (09.08.2026). Текстовий
-- знімок "хто останній змінив роль" + час — навмисно НЕ окрема таблиця
-- логів і НЕ FK на "User" (щоб запис лишався читабельним, навіть якщо
-- того адміна згодом видалять, F.24). Обидва поля NULL для вже наявних
-- рядків — роль ще ніколи не змінювали вручну через /admin/users.

-- AlterTable
ALTER TABLE "User"
  ADD COLUMN "roleChangedByLabel" TEXT,
  ADD COLUMN "roleChangedAt" TIMESTAMP(3);
